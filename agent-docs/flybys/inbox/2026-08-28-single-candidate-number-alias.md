# Accept 1 for a sole uncertain candidate

When one uncertain metadata candidate is shown, the confirmation prompt accepts
Yes but rejects `1`, even though the candidate is visibly numbered `1`. Consider
accepting `1` as an additional Yes alias. Keep the explicit uncertain-match
confirmation and its safe default unchanged.

Status: observed.
Issue: `q` or `quit` at pre-matching inspection continues into metadata lookup.
Rediscover: `Action::Continue` aliases and `inspection_ui::run_before_matching`.
Non-blocking: pre-existing inspection behavior, unrelated to safe Apply.
